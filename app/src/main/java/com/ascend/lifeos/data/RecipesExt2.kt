package com.ascend.lifeos.data

/**
 * RecipeDb extension II — mains, bowls, German classics, shakes (ids 200..299).
 * Same contract as the curated base: macros PER SERVING derived by
 * RecipeDb.build from TOTAL amounts; every line cookable.
 */
internal object RecipesExt2 {

    val ALL: List<RecipeDb.Recipe> = listOf(
    )
}
